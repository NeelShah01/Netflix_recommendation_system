# No files are tracked via Git LFS in this repository.
# Large ML model files (*.pkl) are excluded via .gitignore and regenerated locally.
# Dataset CSV is committed as a regular file (3.4 MB — fine for GitHub).
